<html>

<head>
    <meta property="og:description" content="參與，讓彰師，變得更好。">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= data.url %>" />
    <meta property="og:rich_attachment" content="true" />
    <%- include('../head', {title : data.title + ' | ' + webTitle}) %>
    <link rel="stylesheet" href="/styles/stepBar.css">
</head>

<body>
    <% include ../nav %>
    <div class="container">
        <ol class="breadcrumb">
            <li><a href="/"><i class="fa fa-home fa-2x fa-inverse" aria-hidden="true"></i> 首頁</a></li>
            <li>
                <a href="/<%= data.type %>/">
                    <%= data.typeZh %>
                </a>
            </li>
            <li class="active">
                <%= data.title %>
            </li>
        </ol>
        <header class="page-header">
            <h1><i class="fa fa-hand-rock-o" aria-hidden="true"></i> <%= data.title %></h1>
        </header>
        <ul class="progressbar">
            <li class="active focus">
                <p>提議階段</p>
                <p>提議中</p>
            </li>
            <li><p>附議階段</p></li>
            <li><p>回應階段</p></li>
            <li><p>結案階段</p></li>
        </ul>
        <main>
            <form name="form" enctype="multipart/form-data" method="POST">
                <div class="form-group">
                    <label for="title">標題</label>
                    <input type="text" class="form-control" name="title" placeholder="標題">
                </div>
                <div class="form-group">
                    <label for="content">內容</label>
                    <textarea class="form-control" name="content" rows="3" placeholder="請詳述您遇到的問題，能想出合理的解決方式更好。"></textarea>
                </div>
                <div class="form-group">
                    <label for="imageUpload">上傳照片</label>
                    <input name="image" accept=".png,.jpg,.gif" type="file" id="imageUpload">
                </div>
                <div class="checkbox">
                    <label><input <%= (!user.givenName) ? 'disabled checked' : '' %> name="isAnonymous" type="checkbox"> 匿名發表</label>
                    <% if(!user.givenName) { %>
                        <span style="color:red">若要啟用實名提案，請以學校 Google 帳戶登入。
                    <% } %>
                </div>
                <button type="submit" class="btn btn-default">提案</button>
            </form>
        </main>
    </div>
    <script src="/js/propose.js"></script>
    <%- include ../footer %>
</body>

</html>
